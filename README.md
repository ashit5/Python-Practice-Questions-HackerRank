# Python-List-HackerRank-
Initialize an empty list. Read an integer N representing the number of commands. For each of the N commands, perform the specified list operation (insert, append, remove, sort, pop, reverse, or print) in the given order. Print the list only when the command is print.
